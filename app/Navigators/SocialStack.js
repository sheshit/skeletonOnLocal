/*import * as React from 'react';
import { Text, View, StyleSheet } from 'react-native';
import { Constants } from 'expo';
// or any pure javascript modules available in npm
import { Card } from 'react-native-elements'; 

import { createSwitchNavigator, createStackNavigator, createBottomTabNavigator,createTabNavigator } from 'react-navigation';
import SocialTopTab from './SocialTopTab.js';
import HomeScreen from '../screens/HomeScreen.js'

export const SocialStack = createStackNavigator(
      {
      SocialActivity:SocialTopTab,
      },
      {
        initialRouteName:'SocialActivity',
        navigationOptions: {
      headerStyle: {
        title:'Social Activity',       
        backgroundColor: '#f4511e',
      },
      },
      }
      );
      */